/* eslint-disable */
import withReducer from 'legacy/store/withReducer'
import {{pascalCase name}} from './container'
import reducer from './module'

export default withReducer('{{camelCase name}}', reducer)({{pascalCase name}})
